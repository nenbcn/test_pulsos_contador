# Instrucciones para GitHub Copilot

> Para información específica del proyecto: `README.md`, `ARCHITECTURE.md`, `HARDWARE.md`, `TODO.md`

---

## 📝 Convenciones de Código

### Naming
- **Variables**: `snake_case` - `last_button_time`, `sensor_count`
- **Funciones**: `camelCase` - `updateDisplay()`, `calculateValue()`
- **Constantes**: `MAYÚSCULAS` - `BUTTON_DEBOUNCE_MS`, `MAX_VALUE`
- **Prefijos útiles**: `last_*` (anterior), `current_*` (actual), `*_count` (contadores)

### Estructura de Archivo
```cpp
// 1. Includes
// 2. Defines y constantes
// 3. Variables globales
// 4. Declaraciones forward
// 5. Funciones ISR
// 6. Funciones auxiliares
// 7. setup() y loop()
```

---

## 🔄 Workflow Git/GitHub

### Crear Issues
```bash
gh issue create --title "Descripción clara" --label "enhancement" --body "..."
```

### Commits
**Commits intermedios:**
```bash
git add .
git commit -m "[#N] Paso X: descripción

- Cambio específico
Ref #N"
```

**Commit final (cierra issue):**
```bash
git commit -m "[#N] Título completo

✅ Completado:
- Cambio 1
- Cambio 2

Archivos: src/archivo.cpp
Líneas: +X/-Y
Closes #N"
```

### ⚠️ VERIFICAR ANTES DE COMMIT
```bash
# 1. Guardar archivos (Cmd+S)
# 2. Verificar qué cambió
git status

# 3. Stage cambios
git add .

# 4. Verificar qué se va a commitear
git status  # Debe estar en "Changes to be committed" (verde)
git diff --staged  # Ver cambios exactos

# 5. Commit
git commit -m "..."
```

**IMPORTANTE**: Solo se incluyen archivos **guardados** y **staged** (verde en VS Code).

---

## 🎯 Proceso de Desarrollo

1. **Planificar** → Actualizar `TODO.md` si existe
2. **Crear Issue** → `gh issue create`
3. **Implementar** → Paso a paso, compilando cada cambio
4. **Commit** → Con `Ref #N` (intermedio) o `Closes #N` (final)
5. **Push** → `git push origin main`

---

## 🤖 Para GitHub Copilot

**Antes de modificar código:**
1. Leer `README.md`, `ARCHITECTURE.md` y archivos del proyecto
2. Analizar código existente completo
3. Seguir convenciones establecidas
4. Compilar después de cada cambio
5. No romper funcionalidad existente

**Al generar código:**
- Seguir naming conventions del proyecto
- Mantener estilo consistente
- Comentarios solo cuando sea necesario
- Verificar que compila

**Al hacer commits:**
- Mensaje descriptivo
- Referenciar issue (#N)
- Incluir métricas básicas
- Usar `Closes #N` solo en commit final
